using System;

using GH_Kernel = Grasshopper.Kernel;


namespace Solvers.Parameters
{
    /// <summary>
    /// Exposure of the current subcategory tabs.
    /// </summary>
    enum TabExposure
    {
        GPA = GH_Kernel.GH_Exposure.hidden
    }
}
